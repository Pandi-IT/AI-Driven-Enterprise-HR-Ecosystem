# ==========================================
# REACT FRONTEND LOCAL CONFIGURATIONS
# ==========================================

# API Endpoint for Frontend (uses /api dev-server proxy fallback by default if empty)
VITE_API_URL=http://localhost:8080/api
